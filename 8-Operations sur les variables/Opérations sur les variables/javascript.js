// les opérations sur les variables en JavaScript
let x = 5, y = 10, z = 2;
    x = x + 1;
    x = x - 2;

    y = y * 2;

const mult = x * y;  
const divi =y / z;
let mod = 13 % 3;

alert ("varriable mult:" + mult+
       "\nVariable divi:" + divi +
       "\nVariable mod:" + mod
);

